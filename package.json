{
  "name": "vim-gherkin-formatter",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "bin": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p .",
    "start": "node ./dist/index.js",
    "compile": "yarn run build && yarn run start",
    "install": "yarn global add --prefix ~/.local ./"
  },
  "dependencies": {
    "commander": "^7.2.0",
    "gherkin-io": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "tslib": "^2.2.0",
    "typescript": "^4.2.4"
  }
}
